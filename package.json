{
  "name": "longest-substring",
  "version": "1.0.0",
  "description": "Given a string s, this project will find the length of the longest substring without repeating characters.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Kilelo",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.0.3"
  }
}
